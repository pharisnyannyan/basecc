# 03_checker

Syntax checking stage that builds on the parser AST. This stage parses source
text into an AST using the lexer and parser libraries, then validates the
resulting tree for the expected declaration structure.
